<div id="message-<%= event_message.id %>">
  <small>
    <strong><%= event_message.user.first_name %></strong>
    <i><%= event_message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= event_message.content %></p>
</div>