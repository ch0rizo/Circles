<div id="message-<%= circle_message.id %>">
  <small>
    <strong><%= circle_message.user.first_name %></strong>
    <i><%= circle_message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= circle_message.content %></p>
</div>
